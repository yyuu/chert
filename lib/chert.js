/* chert.js: standard libraries for Chert */

require(_R("dom"));

// depend: dom
require(_R("prototype"));

// depend: prototype
require(_R("kernel"));
require(_R("variable"));
require(_R("string"));
require(_R("dir"));

// depend: string
require(_R("io"));

// depend: io
require(_R("file"));
